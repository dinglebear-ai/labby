name: Repository contract

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: repository-contract-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

permissions: {}

jobs:
  contract:
    permissions:
      contents: read
    uses: dinglebear-ai/workflows/.github/workflows/fleet-contract.yml@b2813662ca27ca8868752fb353d9dd568f2f97f9
    with:
      profile: rust
      implementation-ref: b2813662ca27ca8868752fb353d9dd568f2f97f9
      allow-arm64: true
    secrets: inherit

  repository-contract:
    name: Repository Contract
    if: always()
    needs: [contract]
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    permissions: {}
    steps:
      - name: Require repository contract
        env:
          RESULT: ${{ needs.contract.result }}
        run: |
          if [[ "$RESULT" != "success" ]]; then
            echo "repository contract concluded $RESULT" >&2
            exit 1
          fi
