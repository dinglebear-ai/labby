[package]
name = "verify-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
readme = "../../README.md"
keywords = ["verification", "invariants", "model-checking", "testing"]
categories = ["development-tools"]
publish = false
description = "Invariant catalog, verdict, and target vocabulary for the verification toolkit."

[dependencies]
serde = { workspace = true }
# Data formats only. `serde_json::Value` is in the published ScenarioTarget
# signature and `toml` parses the catalog; neither reaches a transport, the
# filesystem, or the environment, which is the property that actually matters.
serde_json = { workspace = true }
toml = { workspace = true }
thiserror = { workspace = true }

[lints]
workspace = true
