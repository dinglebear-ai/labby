[package]
name = "labby-model"
description = "Executable correctness models for Labby."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false
repository.workspace = true

[dependencies]
serde = { version = "=1.0.228", features = ["derive"] }
serde_json = "=1.0.149"
verify-core = { path = "../../tools/verification/crates/verify-core", version = "=0.1.0" }
verify-scenario = { path = "../../tools/verification/crates/verify-scenario", version = "=0.1.0" }

[lints]
workspace = true
