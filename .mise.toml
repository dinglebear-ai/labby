# Exact pnpm follows the packageManager contracts in apps/gateway-admin and
# apps/palette-tauri. Keep this on 9.15.9 until gateway-admin migrates its
# legacy package.json "pnpm.overrides" block into pnpm-workspace.yaml; pnpm 11
# warns that package.json pnpm settings are no longer read. Palette is already
# on pnpm-workspace.yaml, but this root config must keep gateway-admin safe too.
[tools]
pnpm = "9.15.9"
