# Verification

Require `labby doctor`, health/readiness, persistence, `npx mcporter list` against live MCP, one read-only Labby `help` call, Code Mode status, and clean new logs. Config files alone are not proof. Report evidence and URLs/auth topology without secrets.
