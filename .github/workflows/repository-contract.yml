name: Repository contract

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: repository-contract-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

permissions: {}

jobs:
  contract:
    permissions:
      contents: read
    uses: dinglebear-ai/workflows/.github/workflows/fleet-contract.yml@befa67c7b7f976235bf3fbced6ede93293a7f405 # fleet-2026-08-04
    with:
      profile: rust
      implementation-ref: befa67c7b7f976235bf3fbced6ede93293a7f405
    secrets: inherit

  repository-contract:
    name: Repository Contract
    if: always()
    needs: [contract]
    runs-on: ci-pool-ops
    timeout-minutes: 5
    permissions: {}
    steps:
      - name: Require repository contract
        env:
          RESULT: ${{ needs.contract.result }}
        run: |
          if [[ "$RESULT" != "success" ]]; then
            echo "repository contract concluded $RESULT" >&2
            exit 1
          fi
