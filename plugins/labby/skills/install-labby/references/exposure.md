# Exposure

Keep Labby auth enabled. Funnel: verify the local target, use `tailscale funnel --bg`, check status, synchronize Labby's public URL, and prove persistence. Existing proxy: inspect current docs/config, verify backup/checksum, show exact diff, obtain explicit approval, then verify TLS/origin/auth/OAuth callback.
