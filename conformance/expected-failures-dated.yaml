# Known upstream rust-sdk fixture gap for rmcp 3.1.0 and current main
# commit 830e088d733c7964c806a2305760dd8deb30dff9.
#
# The server-stateless scenario currently fails three SEP-2575 checks:
# - missing clientInfo is accepted instead of returning -32602
# - that invalid request returns HTTP 200 instead of HTTP 400
# - the discover fixture omits mandatory setup fields
#
# The conformance runner is strict: an unlisted failure fails CI, and this
# entry becomes stale and fails CI as soon as the upstream scenario passes.
server:
  - server-stateless
client: []
