# Incus

Confirm a compatible Linux host and working initialized Incus first. Use Labby's supported unprivileged system-container deployment. Verify `boot.autostart=true`, healthy `labby.service`, intended listener, and a live MCP call after restart. Codex App Server inside the container is optional assistance, not MCP.
