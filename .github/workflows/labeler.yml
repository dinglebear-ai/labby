name: Pull request labels

on:
  pull_request_target:
    types: [opened, synchronize, reopened]

concurrency:
  group: labeler-${{ github.event.pull_request.number }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: write

jobs:
  label:
    uses: dinglebear-ai/workflows/.github/workflows/repository-labeler.yml@218eba19f15cc13554d26fa131309cfa8141fd67
    with:
      configuration-path: .github/labeler.yml
      sync-labels: true
