# Custom labels for self-hosted runners so actionlint's runner-label check
# recognizes them. linux-ci-runner carries linux-lab, and windows-ci carries
# windows-lab.
self-hosted-runner:
  labels:
    - unraid
    - linux-lab
    - windows-lab
